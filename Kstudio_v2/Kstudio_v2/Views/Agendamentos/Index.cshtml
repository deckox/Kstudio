@model List<Kstudio_v2.Models.Cliente>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizonta">
        @Html.Raw(ViewData["mensagem"] ?? "")
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            <div class="col-md-10">
                @Html.Label("Lista de Agendamentos")
                @Html.ActionLink("Cadastrar Agendamento", "Cadastro")
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-10">
                @Html.Label("Pesquisar: ")
                <input type="text" name="campoPesquisa" />
                <input type="submit" value="Buscar" class="btn btn-default" />
            </div>
        </div>

        <table class="table">
            <tr>
                <th>
                    @Html.LabelForModel("Id")
                </th>
                <th>
                    @Html.LabelForModel("Banda")
                </th>
                <th>
                    @Html.LabelForModel("Responsável")
                </th>
                <th>
                    @Html.LabelForModel("Data")
                </th>
                <th>
                    @Html.LabelForModel("Horario Inicio")
                </th>
                <th>
                    @Html.LabelForModel("Horario Final")
                </th>
            </tr>

            @foreach (var item in Model)
            {
                var index = 0;
                <tr>
                    <td>
                        @Html.DisplayTextFor(modelItem => item.Id)
                    </td>
                    <td>
                        @Html.DisplayTextFor(modelItem => item.Banda)
                    </td>
                    <td>
                        @Html.DisplayTextFor(modelItem => item.Responsavel)
                    </td>
                    <td>
                        @Html.DisplayTextFor(modelItem => item.Agendamentos[index].Data)
                    </td>
                    <td>
                        @Html.DisplayTextFor(modelItem => item.Agendamentos[index].HorarioInicio)
                    </td>
                    <td>
                        @Html.DisplayTextFor(modelItem => item.Agendamentos[index].HorarioFinal)
                    </td>
                    <td>
                        @Html.ActionLink("Editar", "Editar", new { id = item.Agendamentos[0].Id })
                        @Html.ActionLink("Detalhes", "Detalhes", new { id = item.Agendamentos[0].Id })
                        @Html.ActionLink("Deletar", "Deletar", new { id = item.Agendamentos[0].Id })
                    </td>
                </tr>
            }
        </table>

    </div>


}
